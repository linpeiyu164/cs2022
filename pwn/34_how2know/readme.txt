python3 solve.py

If there are errors in the flag we received, use checkbit_stuck(byte, index, r) and test() functions to further make sure of the results.
This function creates an infinite loop, which makes sure the time difference is infinitely large.
An example is illustrated at the bottom of solve.py.